apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: puter-claim0
  name: puter-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
